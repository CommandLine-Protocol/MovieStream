# ==========================================
# MovieStream Configuration
# ==========================================

# (Optional) TheMovieDB (TMDb) API Key
# If omitted or left blank, MovieStream automatically uses its built-in
# zero-config public metadata resolver (iTunes Search API) and local directory scrapers.
TMDB_API_KEY=
